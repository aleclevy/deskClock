# deskClock
ESP32C3 dev board with I2C screen desk clock! This code uses NTP to time sync and the chip's onboard clock to present the current time. Currently, the code displays in PT, but can be changed. 
